<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/Makeup_image"
        android:layout_width="wrap_content"
        android:src="@mipmap/ic_launcher"
        android:layout_margin="8dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/Makeup_image_desc" />
    <TextView
        android:id="@+id/Makeup_name"
        android:layout_toRightOf="@id/Makeup_image"
        android:layout_marginBottom="8dp"
        style="@style/Makeup_name_style"
        android:layout_toEndOf="@id/Makeup_image" />
    <TextView
        android:id="@+id/Makeup_description"
        android:layout_toRightOf="@id/Makeup_image"
        android:layout_below="@id/Makeup_name"
        style="@style/Makeup_description_style"
        android:layout_toEndOf="@id/Makeup_image" />

</RelativeLayout>